# 贡献指南

感谢参与 DesktopSnapshot。

## 开发原则

- 默认使用中文编写仓库文档和注释；
- 核心逻辑与 Windows API 解耦；
- CLI、GUI 和任务计划都复用同一套核心快照逻辑；
- 不把用户文件上传到网络；
- 对删除、恢复和文件内容备份保持明确、可追踪的状态；
- 修改后至少运行 `cargo fmt --all`、`cargo check --workspace` 和 `cargo test --workspace`。

## 当前优先级

1. 增加系统特殊图标的识别和恢复策略；
2. 增加显示器拓扑变化后的坐标映射；
3. 完善 GUI 快照选择和单项目恢复列表；
4. 增加快照删除、保留和清理策略；
5. 增加内容去重和大文件排除规则；
6. 在安装程序中注册和卸载任务计划；
7. 增加 Windows 集成测试和多显示器测试。
