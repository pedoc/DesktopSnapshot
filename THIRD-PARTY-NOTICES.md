# 第三方声明

## Tauri、Vue、Vite 和 Element Plus

DesktopSnapshot 的图形界面使用 Tauri、Vue、Vite 和 Element Plus 构建。具体许可证以 `crates/ui/package-lock.json` 和对应依赖包附带的许可证为准。

## lnk

DesktopSnapshot 使用 `lnk` crate 解析 Windows `.lnk` 文件。

`lnk` crate 按照 MIT License 发布。具体许可证文本以 Cargo 锁定版本所附带的许可证为准。

本项目自身的源代码按照根目录 `LICENSE` 中的 GPL-3.0-only 许可证发布；该许可证不替代第三方依赖的许可证。
