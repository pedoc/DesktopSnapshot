[package]
name = "desktop-snapshot-storage"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "DesktopSnapshot 的文件系统快照存储"

[dependencies]
desktop-snapshot-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
